//! Audio demo crate for `lambda-rs`.
//!
//! This crate hosts runnable demo binaries under `src/bin/`. It defines a
//! feature surface that maps directly onto high-level `lambda-rs` audio
//! features so each demo can opt into the minimum required capabilities.
