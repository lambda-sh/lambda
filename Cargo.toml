[workspace]

# Sub packages provided by lambda.
members = [
    "lambda",
    "crates/lambda-platform",
    "tools/lambda_rs_demo",
    "tools/minimal",
    "tools/triangles_demo",
]

default-members = [
    "lambda",
    "crates/lambda-platform",
    "tools/lambda_rs_demo"
]
