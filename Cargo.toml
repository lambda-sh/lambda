[workspace]

# Sub packages provided by lambda.
members = [
    "lambda",
    "crates/lambda-platform",
    "crates/lambda-args",
    "tools/obj_loader"
]

default-members = [
    "lambda",
    "crates/lambda-platform",
    "tools/obj_loader"
]
